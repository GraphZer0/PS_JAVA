MusicPlaylist
[x]    addSong(String title) — добавление песни в конец плейлиста;
[x]    removeSong(int index) — удаление песни по индексу;
[x]    moveSong(int from, int to) — перемещение песни с одной позиции на другую;
[x]    shuffle() —  случайное перемешивание песен.

Задача 2: Валидатор HTML
[x]    Напишите программу HTMLValidator, которая проверяет правильность вложенности HTML-тегов в строке. 
[x]    Программа должна проверять, что каждый открывающий тег имеет соответствующий закрывающий тег.

Задача 3: Семейное древо
[x] Создайте класс FamilyMember
[x] addChild(String name) — добавление ребенка;
[x] addParent(String name) — добавление родителя;
[x] findRelatives(String name) — поиск всех родственников;
[x] printTree() — вывод дерева.

